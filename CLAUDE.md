# dotfiles

Instructions for editing this repo are in `AGENTS.md`, next to this file.

Claude Code discovers `CLAUDE.md` but not `AGENTS.md` at either scope (verified
against 2.1.231), so this file exists purely to pull that one in. Keep the
content in `AGENTS.md` — vendor-neutral, and read directly by tools other than
Claude Code.

@AGENTS.md

Not to be confused with `.claude/CLAUDE.md`, which is the *global* instruction
file staged here for deployment to `~/.claude/` — it is not about this repo.
